# Open Graph Assets

Use this folder for page/repository preview images.

Recommended sizes:

- `1200x630` for standard Open Graph previews.
- `1600x900` when the same asset needs a wider crop.

Open Graph images should be legible at small preview size and should carry one
clear subject: person, project, plugin, demo, or proof state.
