# Distribution Assets

Assets in this folder exist for external distribution channels, not only the
portfolio site.

Use this when preparing:

- GitHub README banners and screenshots;
- repository social preview images;
- release/changelog visuals;
- social post crops;
- Open Graph images for public case pages.

Each distribution asset should also be registered in
`docs/assets/asset-manifest.json` when it becomes active or approved.
