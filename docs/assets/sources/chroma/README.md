# Chroma Sources

Store chroma-key source generations here when they are not scoped to a project
folder.

After approval, remove the chroma background locally, crop to alpha bounds, then
export WebP for WordPress or distribution use.
