# Source Assets

Raw or editable sources live here when they are not already better scoped inside
a project-specific folder.

Use:

- `imagegen/` for generated originals;
- `screenshots/` for raw browser, WordPress, Elementor, or GitHub captures;
- `chroma/` for chroma-key source generations.

Sources are preserved for traceability. They are not automatically public or
WordPress-ready.
