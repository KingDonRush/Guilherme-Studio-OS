# Root Leak Quarantine - 2026-06-14

These files were found as loose PNGs in the repository root during the portfolio
visual workflow.

They are preserved here as source/QA screenshots so they are not lost, but they
are not production assets and are not approved WordPress Media Library assets by
default.

Before reusing any file from this folder:

1. classify its project and channel;
2. move or copy it into the correct project asset folder;
3. optimize or convert it only after approval;
4. register it in `docs/assets/asset-manifest.json` if it becomes active,
   approved, implementation-bound, imported, or distribution-bound.

The repository root must remain free of generated images, screenshots, and
handoff exports.
